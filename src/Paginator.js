// import { ReactPaginate } from "react-paginate";

const Paginator = ({ resultsNumber }) => {
  return (
    <div>
      <p>Result number: {resultsNumber}</p>
    </div>
  );
};

export default Paginator;
